{
  "name": "n8n-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Local n8n for AI lead qualification (Gmail → OpenAI → Sheets). Prefer global n8n + start-n8n-local.bat on Windows.",
  "scripts": {
    "start": "start-n8n-local.bat",
    "install:global": "npm.cmd install -g n8n@1.123.71 --no-fund --no-audit"
  },
  "engines": {
    "node": ">=20"
  }
}
